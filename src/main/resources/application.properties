
# Include detailed error msgs
server.error.include-message=always
server.error.include-binding-errors=always

# === PostgreSQL Data Source ===
spring.datasource.url=jdbc:postgresql://localhost:5432/cloudstorage
spring.datasource.username=postgres
spring.datasource.password=28082004

# === MyBatis Configuration ===
mybatis.type-aliases-package=com.udacity.jwdnd.course1.cloudstorage.model
mybatis.configuration.map-underscore-to-camel-case=true

# === JPA/Hibernate Settings (unused, but I'll keep it) ===
# These can be removed if you're not using Spring Data JPA
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.hibernate.boot.allow_jdbc_metadata_access=false
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.properties.hibernate.format_sql=true
# spring.show-sql=true

# === Debug SQL ===
spring.sql.init.mode=always
spring.sql.init.continue-on-error=true
spring.sql.init.schema-locations=classpath:schema.sql
spring.datasource.driver-class-name=org.postgresql.Driver
logging.level.org.apache.ibatis=DEBUG
logging.level.org.springframework.jdbc.core=DEBUG